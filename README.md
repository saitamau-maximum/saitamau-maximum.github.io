# saitamau-maximum.github.io

たぶん今後サイトを作り直すのでそれまでの一般公開用

## Quickstart

Ruby をインストールしておく

```bash
gem install bundler jekyll
bundle install
bundle exec jekyll serve
```

Markdown ファイルには `layout: default` の Front Matter を付ける
